{
  "name": "world-cup-pool",
  "version": "1.0.0",
  "description": "World Cup Pool - Spec Driven Development Workshop",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "test": "pnpm --filter api test",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "setup": "pnpm install && pnpm db:up && pnpm --filter api db:migrate",
    "clean": "pnpm --filter api clean && pnpm --filter web clean"
  },
  "keywords": ["workshop", "spec-driven", "clean-architecture", "node", "vue"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}